from fractions import Fraction

t = int(input())
for i in range(t):
    a, b, c, d = map(int, input().split())

    if Fraction(a, b) + Fraction(c, d) < 1:
        print("YES")
    else:
        print("NO")
