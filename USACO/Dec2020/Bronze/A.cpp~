#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
	vector<int> n(7);
	for (int i=0; i<7; i++) {
		cin>>n[i];
	}
	sort(n.begin(), n.end());
	cout<<n[0]<<" "<<n[1]<<" "<<n[6]-n[0]-n[1];
}
